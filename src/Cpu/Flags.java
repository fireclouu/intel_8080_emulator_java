package Cpu;

public class Flags
{
	// use of boolean data type is considered
	// temporary use of byte to avoid placing if's statement, by directly using data piece of 0 or 1 when needed (e.g. Carry flag)
	// public boolean flag = false;
	
	public byte flag = 0;
}
